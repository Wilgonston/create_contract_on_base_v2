{
  "name": "base-contracts-project",
  "version": "1.0.0",
  "description": "Smart contracts for Base chain",
  "devDependencies": {
    "@openzeppelin/upgrades: "^1.0.0","
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network base"
  }
}
